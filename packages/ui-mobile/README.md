# @familia/ui-mobile

React Native components for FAMILIA. Sprint-0 includes only `Card` and `Text` as a starting point. Full component inventory in [docs/12_COMPONENT_INVENTORY.md](../../docs/12_COMPONENT_INVENTORY.md).

## Resilience contract

Pure UI components — no failure modes.
