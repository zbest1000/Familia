# @familia/tokens

Design tokens (color, type, spacing, radius, motion) shared across mobile and web. Single source of truth.

Imported by `@familia/ui-mobile`, `@familia/ui-web`, `apps/web`, `apps/mobile`. Never duplicate token values.

## Resilience contract

Pure data package — no runtime dependencies, no failure modes.
