apiVersion: v1
kind: Service
metadata:
  name: familia-api
  namespace: familia
  labels:
    app.kubernetes.io/name: familia-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: familia-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
